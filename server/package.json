{
  "name": "medicloud-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node ./dist/server.js",
    "dev": "nodemon --watch server --exec node --experimental-specifier-resolution=node server/server.js",
    "migrate": "node server/migrate.js"
  },
  "dependencies": {
    "better-sqlite3": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
